# php-dep-file-extractor-for-ai
PHP Dependency Extractor (PDE) is a standalone Windows tool for extracting PHP source files and their dependencies from large projects. It automatically discovers class dependencies through framework naming conventions and regex parsing, then copies everything to an isolated folder for AI analysis.
